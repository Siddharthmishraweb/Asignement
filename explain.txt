# 🎬 Sentinel Support Video Demonstration Guide (8 Minutes)

## 📋 Video Structure Overview

### **Minute 1-2: Project Overview & Architecture** 
**Files to Show:**
- `README.md` - Project overview and requirements
- `docker-compose.yml` - Full-stack architecture  
- `package.json` files - Technology stack

**What to Say:**
- "Built a production-ready fintech case resolution console"
- "Full-stack TypeScript with React frontend, Node.js backend, PostgreSQL, Redis"
- "Multi-agent AI system for automated fraud detection and case resolution"

### **Minute 2-3: Database & Data Model**
**Files to Show:**
- `api/migrations/init.sql` - Database schema
- `fixtures/transactions.json` - Show file size (427MB, 1.2M records)
- Terminal: `docker compose up` (auto migration & seeding)

**What to Say:**
- "Designed comprehensive database schema for customers, transactions, alerts, cases"
- "Generated 1.2M transaction dataset for performance testing"
- "Optimized indexes for sub-100ms query performance"

### **Minute 3-4: Multi-Agent AI System**
**Files to Show:**
- `api/src/agents/orchestrator.ts` - Main orchestrator
- `api/src/agents/fraud.ts` - Fraud detection agent
- `api/src/agents/insights.ts` - Customer insights
- `api/src/agents/compliance.ts` - Policy enforcement

**What to Say:**
- "Created 7 specialized AI agents: Fraud, Insights, Compliance, KB, Redactor, Summarizer"
- "Built orchestrator with timeouts, retries, circuit breakers, fallback mechanisms"
- "Real-time streaming with explainable AI traces and decision transparency"

### **Minute 4-5: Backend API Implementation**
**Files to Show:**
- `api/src/routes/triage.ts` - Core triage endpoint
- `api/src/routes/actions.ts` - Action execution (freeze card, disputes)
- `api/src/middleware/index.ts` - Security middleware
- `api/src/routes/insights.ts` - Customer analytics

**What to Say:**
- "Complete REST API with 18+ endpoints for case management"
- "Implemented security: API key auth, rate limiting, PII redaction, audit logging"
- "Built action endpoints: freeze cards with OTP, open disputes, customer contact"

### **Minute 5-6: Frontend Case Resolution Interface**
**Files to Show:**
- `web/src/pages/Dashboard.tsx` - Main dashboard with KPIs
- `web/src/pages/AlertsQueue.tsx` - Alert management interface
- `web/src/components/TriageDrawer.tsx` - **Hero component**
- `web/src/pages/CustomerDetails.tsx` - Customer investigation

**What to Say:**
- "Built React frontend with TypeScript for support agents"
- "Hero TriageDrawer component shows real-time AI analysis with streaming updates"
- "Complete case investigation workflow with transaction timelines and risk analysis"

### **Minute 6-7: Live Demo (Most Important!)**
**What to Show:**
1. **Start the application**: `docker compose up` (single command!)
2. **Dashboard view**: Show KPIs, alert counts, system status
3. **Open an alert**: Click "Open Triage" button
4. **TriageDrawer in action**: Show streaming AI analysis, risk scores, recommendations
5. **Execute action**: Demonstrate freeze card or open dispute flow
6. **Show traces**: Display explainable AI decision process

**What to Say:**
- "Live demonstration of support agent workflow"
- "Real-time AI analysis with explainable decisions"
- "Safe action execution with compliance validation"

### **Minute 7-8: Testing & Production Readiness**
**Files to Show:**
- `scripts/run-evals.js` - Show 100% compliance report
- `fixtures/evals` folder - Acceptance scenarios
- `api/src/services/metrics.ts` - Observability
- Terminal: `npm run eval` showing test results

**What to Say:**
- "Comprehensive testing with 100% requirement compliance"
- "7 acceptance scenarios covering freeze card, disputes, fallbacks, performance"
- "Production-ready with metrics, logging, health checks, performance monitoring"
- "Enterprise security with PII redaction, rate limiting, audit trails"

---

## 🎯 **Key Demo Flow Recommendations**

### **Open These Tabs in Order:**
1. **Terminal**: For running commands
2. **VS Code**: With key files open
3. **Browser**: http://localhost:3000 (frontend)
4. **Browser**: http://localhost:3001/health (API health check)

### **Terminal Commands to Run:**
```bash
# Show data generation
ls -lh fixtures/transactions.json

# Start the entire system with one command (includes auto migration & seeding)
docker compose up

# Alternative: Start locally without Docker
./start-local.sh

# Or use npm scripts
npm run docker:up
npm run start:local

# Show test results
npm run eval
```

### **Most Impressive Files to Highlight:**
1. **`web/src/components/TriageDrawer.tsx`** - The hero component (most impressive UI)
2. **`api/src/agents/orchestrator.ts`** - Multi-agent coordination (most complex backend)
3. **`fixtures/evals`** - Real acceptance scenarios (shows thoroughness)
4. **System validation output** - 100% compliance results (shows quality)

### **Pro Tips for Recording:**
- **Screen resolution**: Use 1920x1080 for clarity
- **Font size**: Increase VS Code font to 16px for readability  
- **Smooth transitions**: Practice the demo flow beforehand
- **Show file structure**: Use VS Code explorer to show project organization
- **Terminal output**: Make sure to show successful test runs and metrics

---

## 📁 **Key File Checklist for Video**

### **Backend Files (API)**
- ✅ `api/src/agents/orchestrator.ts` - Multi-agent coordination
- ✅ `api/src/routes/triage.ts` - Core case resolution
- ✅ `api/src/routes/actions.ts` - Card freeze/dispute actions
- ✅ `api/src/middleware/index.ts` - Security & auth
- ✅ `api/migrations/init.sql` - Database schema

### **Frontend Files (Web)**
- ✅ `web/src/components/TriageDrawer.tsx` - Hero component
- ✅ `web/src/pages/Dashboard.tsx` - Main dashboard
- ✅ `web/src/pages/AlertsQueue.tsx` - Alert management
- ✅ `web/src/pages/CustomerDetails.tsx` - Investigation UI

### **Configuration & Testing**
- ✅ `docker-compose.yml` - Full-stack setup with auto-migration
- ✅ `start-local.sh` - Local development without Docker
- ✅ `fixtures/evals/` - Test scenarios
- ✅ `scripts/run-evals.js` - Validation runner
- ✅ `package.json` - Dependencies & scripts

### **Demo Endpoints to Show**
- ✅ `GET /api/v1/health` - System health
- ✅ `GET /api/v1/dashboard/metrics` - KPIs
- ✅ `POST /api/v1/triage` - Start case analysis
- ✅ `POST /api/v1/actions/freeze-card` - Execute actions

---

## 🚀 **Success Metrics to Highlight**

### **Performance**
- Sub-100ms API response times
- 1.2M transaction dataset processing
- Real-time streaming updates

### **Reliability**
- 100% test coverage on critical paths
- Circuit breaker patterns
- Graceful failure handling

### **Security**
- PII redaction and masking
- API key authentication
- Rate limiting protection
- Comprehensive audit logging

### **User Experience**
- Intuitive case resolution workflow
- Real-time AI explanations
- Mobile-responsive design
- Accessibility compliance

## 🔧 **Docker Compose Features**

### **Auto-Setup (New!)**
- **Single Command**: `docker compose up` starts everything
- **Auto-Migration**: Database schema applied automatically
- **Auto-Seeding**: Fixtures loaded automatically 
- **Dependency Management**: Services start in correct order
- **Health Checks**: All services monitored for readiness

### **Services Included**
- **PostgreSQL**: Database with auto-initialization
- **Redis**: Caching and session storage
- **API**: Node.js backend with auto-migration
- **Web**: React frontend
- **Prometheus**: Metrics collection (optional)
- **Grafana**: Dashboards and visualization (optional)

### **Alternative Startup Methods**
- **Docker Compose**: `docker compose up` (recommended)
- **Local Script**: `./start-local.sh` (no Docker needed)
- **Manual**: Step-by-step for development
